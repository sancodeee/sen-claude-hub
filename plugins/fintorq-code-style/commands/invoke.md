---
description: 调用 fintorq-code-style 审查或修改 Fintorq Java 代码。
argument-hint: "[审查或修改要求；可包含文件、代码片段或目录]"
---

立即调用已安装的 `fintorq-code-style` Skill，并将用户需求原样传入：

$ARGUMENTS

先判断操作边界：

- 用户要求审查、评估、检查或给出意见时，只读取和分析，不修改文件。
- 用户明确要求修复、实现、修改或应用规范时，才进行最小必要改动并验证。

用户指定文件、代码片段或目录时优先处理；未指定范围且当前位于 Git 仓库时，默认检查 tracked diff。只有仍无法可靠定位范围时才询问用户。

按任务内容只读取 Skill 中相关的参考文件。审查结果应包含文件与行号、实际风险和最小修复建议。不要声称执行了未实际运行的 lint 或自动格式化。
